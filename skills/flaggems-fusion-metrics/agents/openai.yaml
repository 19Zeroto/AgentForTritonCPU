interface:
  display_name: "FlagGems Fusion Metrics"
  short_description: "Validate and run FlagGems fusion metrics"
  default_prompt: "Use $flaggems-fusion-metrics to run the serial benchmark runner, build/update the fixed metric map, or export reports from logs."
